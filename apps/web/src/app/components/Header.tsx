import React, { FC } from 'react';
import { StyledHeader } from '../styles/header.style';

const Header: FC = () => {
  return (
    <StyledHeader>
      <h2>TODO</h2>
    </StyledHeader>
  );
};

export default Header;
