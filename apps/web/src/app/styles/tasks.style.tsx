import styled from 'styled-components';

export const StyledSection = styled.section`
  padding: 1rem;
  h2 {
    font-size: 1.5rem;
    font-weight: 300;
    color: var(--grey);
    margin-bottom: 1rem;
  }
`;
